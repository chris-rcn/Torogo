WASM?  C?

Finish migrating to Game2.
Everything is still returning { type, x, y, move } from getMove.
Need to change getMove() consumers to use result.move.
Then remove the type,x,y from the agent result objects.

Ladder priors in tree: failed.
Pattern priors in tree: failed.
Pattern priors in playouts: failed.
Rave-influenced playouts: failed.  Didn't try 

Opening book

Value patterns
Game2 make it cheap to return chain lib/stone counts?
Chain size attribute in pattern?

TD search
  Ramp epsilon
  Why doesn't wide search help?
  Sparse 4x4
  Single-color patterns?

Unify move notation in files.  Use A1 or AA ?


https://www.remi-coulom.fr/CG2010-Simulation-Balancing/SimulationBalancing.pdf

tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 100 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch  10 --filter 0.0 # baseline A'
tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 100 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch  10 --filter 0.0 # baseline B'
tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 200 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch  10 --filter 0.0 # M=200'
tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 300 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch  10 --filter 0.0 # M=300'
tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 500 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch  10 --filter 0.0 # M=500'
tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 100 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch  10 --filter 0.1 # filter=0.1'
tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 100 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch  10 --filter 0.2 # filter=0.2'
tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 100 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch  10 --filter 0.2 # filter=0.3'
tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 100 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch  30 --filter 0.0 # batch=30'
tmuxw 'node train-ppat.js --file out/evals-s9-d20.txt --M 100 --train-pos 0 --test-pos 1000 --lr 10 --test-playouts 1000 --batch 100 --filter 0.0 # batch=100'
C
tmuxw 'c/train_ppat out/evals-s9-100k.txt --M 200 --train-pos 0 --test-pos 0 --lr 10 --test-playouts 1000 --batch  10'
tmuxw 'c/train_ppat out/evals-s9-200k.txt --M 200 --train-pos 0 --test-pos 0 --lr 10 --test-playouts 1000 --batch  10'


Feature definitions:

1. Contiguous to the previous move. Active if the candidate move is among the
8 neighboring points of the previous move. Also active for all features 2–7.
2. Save the string in new atari by capturing. The candidate move that is able
to save the string in new atari by capturing has this feature.
3. Same as Feature 2, which is also self-atari. If the candidate move has Feature 2
but is also a self-atari, then instead it has Feature 3 (Fig. 1).
4. Save the string in new atari by extension. The candidate move that is able
to save the string in new atari by extension has this feature.
5. Same as Feature 4, which is also self-atari.
6. Solve a new ko by capturing. If there is a new ko, then the candidate move
that is able to solve the ko by capturing any one of the neighboring strings
has this feature.
7. 2-point semeai. If the previous move reduces the liberties of a string to only
two, then the candidate move that is able to kill its neighboring string by
giving atari has this feature. Fig. 1 gives an example. This feature deals with
the most basic type of semeai.

Expected feature e^x:
1: 7.43
2: 151.04
3: 0.53
4: 23.11
5: 0.02
6: 6.37
7: 141.80

